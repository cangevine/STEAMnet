<%= form_for(@spark) do |f| %>
  <% if @spark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spark.errors.count, "error") %> prohibited this spark from being saved:</h2>

      <ul>
      <% @spark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :spark_type %><br />
    <%= f.text_field :spark_type %>
  </div>
  <div class="field">
    <%= f.label :content_type %><br />
    <%= f.text_field :content_type %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :content_hash %><br />
    <%= f.text_area :content_hash %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
