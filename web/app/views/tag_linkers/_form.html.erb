<%= form_for(@tag_linker) do |f| %>
  <% if @tag_linker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_linker.errors.count, "error") %> prohibited this tag_linker from being saved:</h2>

      <ul>
      <% @tag_linker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
